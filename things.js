module.exports.array_counter = function (array) {
    return "В массиве находится " + array.length + " элементов!";
}

module.exports.multiply = function (x, y) {
    return `${x} умножить ${y} ${x * y}`;
}

module.exports.some_value = 522;


